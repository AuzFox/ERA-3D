// waves.ec by Auz
// simple dynamic mesh waves

enum {
    QUADS = 2,

    OMEM_ADDR = 0x00C00000,

    BLACK = 0x000000FF
};

void generateMesh() {
    for (int y = 0; y < GRID_H; y = y + 1) {
        for (int x = 0; x < GRID_W; x = x + 1) {
            //
        }
    }
}

void init() {
    generateMesh();
}
