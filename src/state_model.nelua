require("statemachine")

-- TODO: add basic ui to load model files

global function stateModelInit(this: *State, sm: *StateMachine, vm: *e3d.EVM): void
end

global function stateModelUpdate(this: *State, sm: *StateMachine, vm: *e3d.EVM): integer
    return StateID.MODEL
end

global function stateModelDraw(this: *State, sm: *StateMachine, vm: *e3d.EVM): integer
    return StateID.MODEL
end
