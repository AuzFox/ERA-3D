require("statemachine")

-- TODO: boot animation

local m: rl.model

global function stateBootInit(this: *State, sm: *StateMachine, vm: *e3d.EVM): void
end

global function stateBootUpdate(this: *State, sm: *StateMachine, vm: *e3d.EVM): integer
    return StateID.BOOT
end

global function stateBootDraw(this: *State, sm: *StateMachine, vm: *e3d.EVM): integer
    return StateID.BOOT
end
